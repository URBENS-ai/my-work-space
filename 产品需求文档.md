# 个人工作台 · 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 更新日期 | 2026-08-19 |
| 状态 | 待评审 |
| 技术形态 | 纯前端单文件 HTML（无需安装、无需构建，双击即用） |

---

## 1. 产品概述

### 1.1 背景

自由职业者每天开工时，需要在多个工具之间切换来管理任务、查看日程、打开常用网站，信息分散、成本高。「个人工作台」把这三件事收敛到一个页面，作为每天开机的起点。

### 1.2 目标用户

- 自由职业者、独立工作者、个体经营者。
- 需要一张「每日起点页」：看任务、看日程、跳常用网站。

### 1.3 使用场景

- 每天早上打开工作台，先扫一眼「今日待办」和「月历」，再点常用工具入口开始工作。
- 临时想起一件事，在待办清单里快速新增并设优先级。
- 定了某天的日程，在月历上标记。
- 定期导出 JSON 备份，换电脑或重装后用导入恢复。

### 1.4 产品目标

- 一个页面承载「待办 + 月历 + 工具入口」三大能力。
- 所有编辑通过弹窗完成，不做多页跳转。
- 数据完全本地化，支持 JSON 备份与恢复。

### 1.5 范围

**包含（In Scope）**

- 待办清单：增、删、改、查、优先级、完成标记。
- 月历视图：浏览任意月份、标记日程、联动显示待办截止日。
- 常用工具入口：保存网址 + Emoji 图标，点击跳转。
- 数据：localStorage 本地存储，导出 / 导入 JSON。

**不包含（Out of Scope，本期不做）**

- 云同步、多端同步、账号体系。
- 提醒 / 通知。
- 多页面、多工作区。
- 移动端单独适配（以桌面浏览器为主）。

---

## 2. 功能需求

### 2.1 待办清单

**功能描述**

管理工作任务，支持增删改查、设置优先级、标记完成。

**使用方式**

1. **新增**：点击「新增待办」按钮，弹出待办编辑弹窗，填写标题，可选设置优先级、截止日期，保存后加入清单。
2. **查看**：待办以列表展示，按「未完成优先、优先级从高到低、创建时间」排序；每条显示优先级标签、标题、截止日期、完成状态。
3. **编辑**：点击某条待办，弹出编辑弹窗，修改标题 / 优先级 / 截止日期。
4. **完成**：点击待办前的勾选框切换「已完成 / 未完成」；已完成项置灰并显示删除线，排到列表末尾。
5. **删除**：在编辑弹窗内或列表项上提供「删除」，删除前弹确认。

**优先级定义（三档）**

| 档位 | 标识 | 展示 |
| --- | --- | --- |
| 高 | high | 红色标签 / 置顶排序 |
| 中 | medium | 橙色标签 / 中间排序 |
| 低 | low | 灰色标签 / 底部排序 |

**实现要点**

- 每条待办字段：`id`、`title`、`priority`、`dueDate`（可空）、`done`、`createdAt`。
- 排序规则：`done` 升序 → 优先级权重（高>中>低）→ `createdAt` 升序。
- 有 `dueDate` 的待办自动出现在月历对应日期（见 2.2 联动）。

---

### 2.2 月历视图

**功能描述**

以月历网格展示当前月份，支持标记日程，并联动展示待办截止日。

**使用方式**

1. **切换月份**：顶部提供「上月 / 下月」按钮，可回到「今天」。
2. **标记日程**：点击某一天，弹出「新增日程」弹窗，填写标题，可选时间、备注，保存后该日期格显示日程。
3. **查看日程**：日期格内列出当天日程；待办截止日也以不同样式显示在该格。
4. **编辑 / 删除日程**：点击已存在的日程项，弹出编辑弹窗，可修改或删除。
5. **今天高亮**：今天的日期格高亮标识。

**联动规则（日程与待办）**

- 待办清单中带 `dueDate` 的待办，自动显示在月历对应日期格内，样式与「日程」区分（如不同颜色的小圆点 / 前缀图标）。
- 在月历上查看某天，可同时看到「该天的日程」和「该天到期的待办」。
- 日程为独立数据，不会反向出现在待办清单中。

**实现要点**

- 纯原生 JS 根据「年 + 月」计算当月天数、首日星期，渲染 7 列网格。
- 每月数据从 `todos`（按 `dueDate`）与 `events`（按 `date`）聚合。
- 跨月跳转只需重算并重渲染，不涉及多页。

---

### 2.3 常用工具入口

**功能描述**

保存常用网站，用 Emoji 作为图标，点击新标签页打开。

**使用方式**

1. **新增**：点击「新增工具」，弹出编辑弹窗，选择 Emoji、填写名称、填写网址，保存后以卡片 / 图标形式展示。
2. **打开**：点击入口，在新标签页打开对应网址。
3. **编辑 / 删除**：悬停或右键入口提供「编辑 / 删除」；删除前弹确认。
4. **排序**：本期按创建时间排序（可选，暂不做拖拽）。

**实现要点**

- 每条字段：`id`、`emoji`、`name`、`url`、`createdAt`。
- Emoji 由用户直接输入或从内置常用 Emoji 面板选择（如 ✅ 🎨 📧 📝 💰 📅 🔗）。
- 打开方式：`window.open(url, '_blank')`；URL 未带协议时自动补 `https://`。
- 需对 URL 做基本合法性校验，避免注入非法链接。

---

### 2.4 数据存储与备份

**功能描述**

全部数据存本地，支持导出 JSON 备份、导入 JSON 恢复。

**使用方式**

1. **自动保存**：所有增删改自动写入本地存储，无需手动保存。
2. **导出备份**：点击「导出备份」，生成 `workbench-backup-YYYYMMDD.json` 并触发浏览器下载。
3. **导入恢复**：点击「导入备份」，选择 JSON 文件，校验通过后恢复数据。

**实现要点**

- 存储介质：浏览器 `localStorage`，键名建议 `workbench.data.v1`。
- 数据以统一 JSON 结构存储（见第 3 节数据模型），包含版本号。
- 导出：`JSON.stringify` → 创建 `Blob` → 生成临时下载链接触发下载。
- 导入：`<input type="file">` + `FileReader` 读取 → `JSON.parse` → 结构校验 → 写入存储。
- 导入策略：**覆盖式恢复**（以备份文件内容替换当前数据）。导入前弹确认，明确提示「将覆盖当前数据」。

---

## 3. 数据模型

```json
{
  "version": 1,
  "todos": [
    {
      "id": "uuid",
      "title": "string",
      "priority": "high | medium | low",
      "dueDate": "YYYY-MM-DD | null",
      "done": false,
      "createdAt": 1720000000000
    }
  ],
  "events": [
    {
      "id": "uuid",
      "title": "string",
      "date": "YYYY-MM-DD",
      "time": "HH:mm | null",
      "note": "string | null",
      "createdAt": 1720000000000
    }
  ],
  "links": [
    {
      "id": "uuid",
      "emoji": "string",
      "name": "string",
      "url": "string",
      "createdAt": 1720000000000
    }
  ]
}
```

- `id`：使用 `crypto.randomUUID()` 生成，保证唯一。
- `createdAt`：毫秒时间戳。
- `version`：用于导入时校验与后续兼容升级。

---

## 4. 页面结构

- **单主页**，自上而下 / 分区布局：
  1. **顶栏**：产品名 + 「导出备份」「导入备份」入口。
  2. **待办清单区**：待办列表 + 「新增待办」。
  3. **月历视图区**：月历网格 + 月份切换。
  4. **常用工具区**：工具入口卡片 + 「新增工具」。
- **弹窗（Modal）**，统一承载所有编辑操作：
  - 待办新增 / 编辑弹窗
  - 日程新增 / 编辑弹窗
  - 工具新增 / 编辑弹窗
  - 删除确认弹窗
- 所有弹窗支持「保存 / 取消」，取消不保存改动。

---

## 5. 非功能需求

- **可用性**：桌面浏览器双击打开即可使用，无网络依赖。
- **兼容性**：现代浏览器（Chrome / Edge / Firefox 最新版），依赖 `crypto.randomUUID`、`localStorage`、`Blob`、`FileReader`。
- **数据安全**：数据仅存本机，不做任何上传；导出文件为明文 JSON，注意自行保管。
- **健壮性**：导入时校验 JSON 结构，非法文件给出明确报错且不破坏现有数据。

---

## 6. 异常与边界

| 场景 | 处理 |
| --- | --- |
| 待办标题为空 | 禁止保存，提示「标题不能为空」 |
| 工具网址不合法 | 禁止保存，提示「请输入合法网址」 |
| 导入非法 JSON / 结构不符 | 提示错误，不写入、不覆盖现有数据 |
| 日期格无任何内容 | 显示空状态，点击仍可新增日程 |
| 月份跨年 | 上月 / 下月正常跨越年份 |

---

## 7. 待确认 / 可调整项

以下为本次对齐的结论，若后续想调整，改动成本低：

- 优先级为固定三档「高/中/低」，如需四象限可后续升级。
- 导入策略为「覆盖式恢复」，如需「合并导入」可后续补充。
- 本期不做拖拽排序、提醒通知、云同步。
